# Tuned postgresql.conf for mempalace-db running inside a 6 GiB cgroup.
#
# This file is mounted onto /etc/postgresql/postgresql.conf inside the container
# and referenced via `-c config_file=` in docker-compose.yml's command. Sized for
# the familiar host (15 GiB total RAM, shared with llama-server + palace-daemon +
# familiar-api + kg-extract). See docker-compose.yml's `mem_limit` block for the
# host-budget reasoning.

# --- Connections ----------------------------------------------------------

listen_addresses = '*'
max_connections = 200

# --- Memory (the load-bearing changes) ------------------------------------

# Pre-2026-05-28: 4GB. Could not fit inside the 3g cgroup — postgres OOMed
# repeatedly trying to page in its own shared region. 2GB leaves room inside
# the 6g cap for per-backend work_mem, AGE traversal sorts, and the OS file
# cache that effective_cache_size advertises.
shared_buffers = 2GB

# Pre-2026-05-28: 12GB. The familiar host only has 15 GiB total and ~3 GiB
# of it goes to llama-server, ~1 GiB to palace-daemon, plus the OS — there
# was never 12 GiB of free page cache available to postgres. 6GB is the
# realistic ceiling for what the kernel can keep hot for us, and matches
# the cgroup envelope.
effective_cache_size = 6GB

# Per-backend sort/hash memory. 32 MB × 200 connections = 6.4 GiB worst case —
# already a latent risk at the old 3g cap, still tight at 6g. Holding the
# value steady (most queries don't allocate the full work_mem); revisit if
# we see sort spills or per-query memory pressure under load.
work_mem = 32MB

maintenance_work_mem = 512MB

# --- WAL ------------------------------------------------------------------

max_wal_size = 4GB
min_wal_size = 256MB
wal_buffers = 64MB
synchronous_commit = off
wal_writer_delay = 200ms

# --- Parallelism ----------------------------------------------------------

max_worker_processes = 16
max_parallel_workers = 12
max_parallel_workers_per_gather = 4
max_parallel_maintenance_workers = 4

# --- Shared memory --------------------------------------------------------

dynamic_shared_memory_type = posix

# --- Locale / formatting --------------------------------------------------

log_timezone = 'Etc/UTC'
datestyle = 'iso, mdy'
timezone = 'Etc/UTC'
lc_messages = 'en_US.UTF-8'
lc_monetary = 'en_US.UTF-8'
lc_numeric = 'en_US.UTF-8'
lc_time = 'en_US.UTF-8'
default_text_search_config = 'pg_catalog.english'
