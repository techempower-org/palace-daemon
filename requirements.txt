mempalace>=3.3.2
fastapi>=0.136.0
uvicorn[standard]>=0.44.0
watchdog>=3.0.0
# Required when MEMPALACE_BACKEND=postgres. Small (~3 MB) and only loaded on
# demand by mempalace's postgres backend, so safe to ship unconditionally.
# See palace-daemon#15: degraded-state at runtime is worse than 3 MB on disk.
psycopg2-binary>=2.9
# psycopg (v3) — mempalace.knowledge_graph_age uses this for Cypher
# execution. Already a transitive dep via mempalace, but listed
# explicitly so the test venv has it: the tests for /cypher's structured
# error mappings need to catch v3 exception classes (PR #163), and
# without psycopg installed locally those tests skip silently, letting
# a future library-version drift regress unnoticed.
psycopg[binary]>=3.1
# Neural reranking — FlashRank nano (ms-marco-TinyBERT-L-2-v2, ~4 MB ONNX,
# CPU-friendly). Spike for techempower-org/familiar.realm.watch#43. Gated
# by PALACE_RERANK_ENABLED (default: true). Fall-through is graceful: if
# import or model load fails, /search* still returns the BM25/vector
# ordering unchanged.
#
# EXACT pin (not a floor): the rerank stage is deterministic given a fixed
# model (proven in docs/evals/2026-05-30-retrieval-determinism.md), but a
# newer flashrank could ship different bundled ONNX weights or a different
# tokenizer — which would silently reorder /search top-5 across a deploy.
# Pinning the version + an explicit PALACE_RERANK_MODEL (set in
# palace-daemon.service) keeps deployed ranking byte-stable across deploys.
# Bump deliberately, re-run tests/test_rerank.py::TestRerankDeterminism.
flashrank==0.2.10
