# Logs and local config
*.log
.gemini/*
!.gemini/config.yaml
.claude/

# Site-local deploy config (PALACE_* overrides) — keep the .example tracked.
scripts/deploy.conf
scripts/auto-repair.conf

# Python
__pycache__/
*.py[cod]
mempalace.yaml
entities.json
*.log
.env
venv/
